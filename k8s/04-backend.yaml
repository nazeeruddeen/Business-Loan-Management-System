apiVersion: apps/v1
kind: Deployment
metadata:
  name: business-loan-backend
  namespace: business-loan
spec:
  replicas: 2
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: business-loan-backend
  template:
    metadata:
      labels:
        app: business-loan-backend
    spec:
      terminationGracePeriodSeconds: 30
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                topologyKey: kubernetes.io/hostname
                labelSelector:
                  matchLabels:
                    app: business-loan-backend
      containers:
        - name: backend
          image: business-loan-management-system:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8010
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8010
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8010
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8010
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
          envFrom:
            - configMapRef:
                name: business-loan-config
            - secretRef:
                name: business-loan-secret
---
apiVersion: v1
kind: Service
metadata:
  name: business-loan-backend
  namespace: business-loan
spec:
  selector:
    app: business-loan-backend
  ports:
    - port: 80
      targetPort: 8010
