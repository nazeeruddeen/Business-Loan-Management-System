apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: business-loan-mysql-pvc
  namespace: business-loan
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: business-loan-mysql
  namespace: business-loan
spec:
  replicas: 1
  selector:
    matchLabels:
      app: business-loan-mysql
  template:
    metadata:
      labels:
        app: business-loan-mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.4
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: business-loan-secret
                  key: SPRING_DATASOURCE_PASSWORD
            - name: MYSQL_DATABASE
              value: business_loan_management
          volumeMounts:
            - name: mysql-data
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-data
          persistentVolumeClaim:
            claimName: business-loan-mysql-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: business-loan-mysql
  namespace: business-loan
spec:
  selector:
    app: business-loan-mysql
  ports:
    - port: 3306
      targetPort: 3306
