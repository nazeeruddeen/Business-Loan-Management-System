apiVersion: apps/v1
kind: Deployment
metadata:
  name: business-loan-prometheus
spec:
  replicas: 1
  selector:
    matchLabels:
      app: business-loan-prometheus
  template:
    metadata:
      labels:
        app: business-loan-prometheus
    spec:
      containers:
        - name: prometheus
          image: quay.io/prometheus/prometheus:v2.53.0
          args:
            - --config.file=/etc/prometheus/prometheus.yml
            - --storage.tsdb.path=/prometheus
          ports:
            - containerPort: 9090
              name: http
          readinessProbe:
            httpGet:
              path: /-/ready
              port: http
          livenessProbe:
            httpGet:
              path: /-/healthy
              port: http
          volumeMounts:
            - name: config
              mountPath: /etc/prometheus
            - name: storage
              mountPath: /prometheus
      volumes:
        - name: config
          configMap:
            name: business-loan-prometheus-config
        - name: storage
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: business-loan-prometheus
spec:
  selector:
    app: business-loan-prometheus
  ports:
    - port: 9090
      targetPort: http
      name: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: business-loan-alertmanager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: business-loan-alertmanager
  template:
    metadata:
      labels:
        app: business-loan-alertmanager
    spec:
      containers:
        - name: alertmanager
          image: quay.io/prometheus/alertmanager:v0.28.1
          args:
            - --config.file=/etc/alertmanager/alertmanager.yml
            - --storage.path=/alertmanager
            - --web.external-url=http://localhost:9093
          ports:
            - containerPort: 9093
              name: http
          readinessProbe:
            httpGet:
              path: /-/ready
              port: http
          livenessProbe:
            httpGet:
              path: /-/healthy
              port: http
          volumeMounts:
            - name: config
              mountPath: /etc/alertmanager
            - name: email-secret
              mountPath: /etc/alertmanager/secrets
              readOnly: true
            - name: storage
              mountPath: /alertmanager
      volumes:
        - name: config
          configMap:
            name: business-loan-alertmanager-config
        - name: email-secret
          secret:
            secretName: business-loan-alertmanager-email
            optional: true
        - name: storage
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: business-loan-alertmanager
spec:
  selector:
    app: business-loan-alertmanager
  ports:
    - port: 9093
      targetPort: http
      name: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: business-loan-alert-receiver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: business-loan-alert-receiver
  template:
    metadata:
      labels:
        app: business-loan-alert-receiver
    spec:
      containers:
        - name: alert-receiver
          image: mendhak/http-https-echo:31
          env:
            - name: HTTP_PORT
              value: '8080'
          ports:
            - containerPort: 8080
              name: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          livenessProbe:
            httpGet:
              path: /
              port: http
---
apiVersion: v1
kind: Service
metadata:
  name: business-loan-alert-receiver
spec:
  selector:
    app: business-loan-alert-receiver
  ports:
    - port: 8080
      targetPort: http
      name: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: business-loan-grafana
spec:
  replicas: 1
  selector:
    matchLabels:
      app: business-loan-grafana
  template:
    metadata:
      labels:
        app: business-loan-grafana
    spec:
      containers:
        - name: grafana
          image: grafana/grafana:11.1.0
          env:
            - name: GF_SECURITY_ADMIN_USER
              value: admin
            - name: GF_SECURITY_ADMIN_PASSWORD
              value: admin
          ports:
            - containerPort: 3000
              name: http
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
          volumeMounts:
            - name: datasources
              mountPath: /etc/grafana/provisioning/datasources
            - name: dashboards-provider
              mountPath: /etc/grafana/provisioning/dashboards
            - name: dashboard
              mountPath: /var/lib/grafana/dashboards
      volumes:
        - name: datasources
          configMap:
            name: business-loan-grafana-datasources
        - name: dashboards-provider
          configMap:
            name: business-loan-grafana-dashboards-provider
        - name: dashboard
          configMap:
            name: business-loan-grafana-dashboard
---
apiVersion: v1
kind: Service
metadata:
  name: business-loan-grafana
spec:
  selector:
    app: business-loan-grafana
  ports:
    - port: 3000
      targetPort: http
      name: http
